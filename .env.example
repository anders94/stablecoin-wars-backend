# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=stablecoin_wars

# Redis (for Bull job queue and rate limiting)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password

# RPC Rate Limiting (requests per second, supports floats like 10.5)
DEFAULT_RPC_RATE_LIMIT=10

# API
PORT=3000
NODE_ENV=development
